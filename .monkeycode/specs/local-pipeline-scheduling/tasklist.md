# 需求实施计划

- [x] 1. 实现本地调度领域模型与持久化存储
  - [x] 1.1 在 `elyra/pipeline/local/` 中实现调度、运行记录和日志条目的数据模型，以及 5 段 Cron 校验和 JSON 序列化，覆盖需求 1.1、1.2、3.1、4.1。
  - [x] 1.2 实现文件存储、原子写入和运行记录保留清理，保留最近 100 次及 90 天内记录，并同步清理关联日志，覆盖需求 1.3、1.4、3.5、4.5。
  - [x] 1.3 为模型往返序列化、Cron 校验和保留策略编写单元测试，验证设计正确性属性 4、6。

- [x] 2. 实现服务端调度与本地执行日志
  - [x] 2.1 实现由 Jupyter Server 生命周期管理的 `LocalPipelineScheduler`，加载启用任务、计算下次触发、恢复后从当前时刻调度，以及单任务活动运行限制，覆盖需求 1.5、2.1、2.2、2.3。
  - [x] 2.2 扩展 `LocalPipelineProcessor`，将管道和节点的开始、成功、失败事件写入关联运行日志，覆盖需求 2.4、4.2、4.4。
  - [x] 2.3 在 `ElyraApp` 初始化与停止阶段启动和关闭调度服务，覆盖需求 1.5、2.3。
  - [x] 2.4 为计划触发、重叠跳过、重启恢复和节点级日志编写单元测试，验证设计正确性属性 1、2、3。

- [x] 3. 提供调度、历史和日志 REST API
  - [x] 3.1 创建受 Jupyter Server 认证保护的本地调度任务 CRUD handler，并复用 `PipelineValidationManager` 进行管道定义验证，覆盖需求 1.1 至 1.4、5.4。
  - [x] 3.2 创建运行记录查询与日志读取 handler，提供任务、状态、时间筛选和按顺序日志响应，覆盖需求 3.1、3.2、4.3。
  - [x] 3.3 为 handler 的成功、验证失败、存储失败和认证路径编写 Tornado 单元测试。

- [x] 4. 检查点 - 本地调度测试全部通过；全量后端套件的两个 metadata 权限用例受 root 运行环境限制

- [x] 5. 在 Pipeline Editor 提供本地调度管理界面
  - [x] 5.1 新增 `LocalScheduleService`，集中管理调度任务、运行记录和日志 API 调用，覆盖需求 5.4。
  - [x] 5.2 为 Local 运行时新增创建、编辑、启用、删除和查看下次触发时间的交互界面，覆盖需求 5.1、5.2。
  - [x] 5.3 新增运行历史列表和只读日志面板，覆盖需求 3.3、4.3、5.3。
  - [x] 5.4 为服务客户端与调度管理组件编写 Jest 单元测试。
  - [ ]* 5.5 为创建任务、查看历史和日志编写 Cypress 集成测试。

- [x] 6. 检查点 - 本地调度前后端测试与 TypeScript 编译通过；全量后端套件的两个 metadata 权限用例受 root 运行环境限制
