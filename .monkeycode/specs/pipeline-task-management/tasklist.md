# 需求实施计划

- [x] 1. 扩展本地任务、运行和结果持久化模型
  - [x] 1.1 在 `elyra/pipeline/local/models.py`、`schedule_store.py` 和 `run_store.py` 中实现用户归属、可自定义重试策略、运行触发来源、尝试链和结果元数据，并兼容现有调度数据。对应需求 2.1-2.4、3.1、3.6、4.2、5.1-5.2、6.3-6.4。
  - [x] 1.2 为模型校验、存储往返、用户隔离、重试策略默认值和结果归属编写 pytest 测试。验证设计正确性属性 1、2、3、6。

- [x] 2. 实现调度、自动重试、手动重试和停止控制
  - [x] 2.1 扩展 `elyra/pipeline/local/scheduler.py` 和 `local_processor.py`，实现指数退避重试队列、手动重试链、运行停止和 Enterprise Gateway Kernel 执行元数据。对应需求 3.2-3.5、4.1-4.2、6.1-6.2。
  - [x] 2.2 为重试延迟、最大尝试次数、手动重试、服务恢复、活动运行停止和远程 Kernel 元数据编写 pytest 测试。验证设计正确性属性 3、4、5、7。

- [x] 3. 扩展任务管理 REST API
  - [x] 3.1 在 `elyra/pipeline/local/handlers.py` 和 `elyra/elyra_app.py` 中提供任务列表、详情、手动运行、运行详情、重试、停止、结果和运行记录删除端点，并对每个请求执行用户归属验证。对应需求 1.1-1.3、3.3-3.4、4.3-4.4、5.2、6.3-6.4。
  - [x] 3.2 为 API 过滤、归属隔离、重试策略校验、停止、结果读取和运行删除编写 Tornado handler 测试。验证设计正确性属性 1、2、5、6。

- [ ] 4. 实现 JupyterLab 任务管理界面
  - [x] 4.1 将 `LocalSchedulesWidget` 演进为任务管理侧栏，在 `packages/pipeline-editor` 中实现任务筛选、计划编辑、重试策略编辑、手动运行、运行详情、停止、重试、日志、结果和删除操作。对应需求 1.1-1.3、2.1-2.4、3.1-3.4、4.1-4.4、5.1-5.3。
  - [ ] 4.2 为任务服务请求、筛选、重试参数、运行控制、结果展示和删除操作编写 Jest 测试。对应需求 1.1-1.3、3.1-3.4、4.1-4.4、5.1-5.3。

- [ ] 5. 检查点 - 确保所有测试通过,如有疑问请询问用户
  - [ ] 5.1 运行 Local Pipeline pytest 测试和 Pipeline Editor Jest 测试，修复实现引入的回归。对应全部需求与设计测试策略。
